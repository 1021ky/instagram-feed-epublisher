# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so

# Env
.venv/
venv/
.env
.python-version

# Build artifacts
build/
dist/
*.egg-info/

# Tools caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage

# macOS
.DS_Store

# VSCode project settings (keep tasks, so don't ignore .vscode by default)
# .vscode/

# Jupyter
.ipynb_checkpoints/

# Project-specific
auth.json
# Instaloaderが生成するセッションファイルを個別に指定
# ワイルドカード（session-*）だと将来追加される同名の別ファイル（.json、.mdなど）まで
# 巻き込んで無視してしまうため、既知のファイルのみを明示的に指定
session-vaivailx
*.epub
posts_data.json

# Test compatibility alias modules (auto-generated for testing)
commands.py
config.py
utils.py
epub_builder.py
instagram_fetch.py

# NextJS
node_modules/
.next/

