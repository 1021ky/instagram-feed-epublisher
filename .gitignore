# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so

# Env
.venv/
venv/
.env
.python-version

# Build artifacts
build/
dist/
*.egg-info/

# Tools caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage

# macOS
.DS_Store

# VSCode project settings (keep tasks, so don't ignore .vscode by default)
# .vscode/

# Jupyter
.ipynb_checkpoints/

# Project-specific
auth.json
# Instaloaderが生成するセッションファイル (session-<username>) を無視
# ただし、アプリケーションコードの session-service.ts は除外（下記の!で許可）
session-*
# session-* パターンが session-service.ts にもマッチしてしまうため、
# アプリケーションのソースコードとして必要な session-service.ts を明示的に許可
!webapp/src/lib/auth/session-service.ts
*.epub
posts_data.json

# Test compatibility alias modules (auto-generated for testing)
commands.py
config.py
utils.py
epub_builder.py
instagram_fetch.py

# NextJS
node_modules/
.next/

